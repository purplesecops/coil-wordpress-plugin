version: "3.8"
services:
    db:
        image: mysql
        env_file:
            - ./mysql.env
    php:
        build:
            context: .
            dockerfile: ./scripts/php.dockerfile
        working_dir: /var/www/html
        depends_on:
            - db